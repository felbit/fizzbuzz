(ns fizzbuzz.core)

(defn fizzbuzz
  "Returns
   - 'fizz' for every input dividable by 3,
   - 'buzz' for every input dividable by 5,
   - both if both is true; and
   - the input otherwise."
   [n]
   n)
